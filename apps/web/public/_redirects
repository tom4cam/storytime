# SPA fallback for Cloudflare Pages: any path that isn't a static asset
# or a /api/* function (which Pages auto-routes from functions/api/*.ts)
# falls through to index.html so React Router handles it client-side.
/*  /index.html  200
